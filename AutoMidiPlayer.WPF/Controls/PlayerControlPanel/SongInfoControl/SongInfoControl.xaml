<UserControl x:Class="AutoMidiPlayer.WPF.Controls.PlayerControlPanel.SongInfoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="0,0,12,0"
          VerticalAlignment="Center"
          HorizontalAlignment="Stretch"
          ClipToBounds="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Width="48"
                Height="48"
                CornerRadius="4"
                Background="{DynamicResource ControlFillColorDefaultBrush}">
            <ui:SymbolIcon Symbol="MusicNote216"
                           Filled="True"
                           FontSize="22"
                           Opacity="0.4"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
        </Border>
        <StackPanel Grid.Column="1"
                    Margin="12,0,0,0"
                    VerticalAlignment="Center">
            <TextBlock Text="{Binding TrackView.Queue.OpenedFile.Title, FallbackValue=No song playing}"
                       FontWeight="SemiBold"
                       FontSize="14"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="NoWrap"/>
            <TextBlock Text="{Binding TrackView.Queue.OpenedFile.Song.Artist, FallbackValue=Unknown artist}"
                       Opacity="0.6"
                       FontSize="12"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="NoWrap"/>
        </StackPanel>
    </Grid>
</UserControl>
