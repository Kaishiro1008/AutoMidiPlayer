<UserControl x:Class="AutoMidiPlayer.WPF.Controls.SelectorPopupButton.Button.StandardDropdown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:AutoMidiPlayer.WPF.Controls.SelectorPopupButton.Button"
             xmlns:menu="clr-namespace:AutoMidiPlayer.WPF.Controls.SelectorPopupButton.Menu"
             x:Name="Root"
             Focusable="False">
    <Grid ToolTip="{Binding ToolTip, ElementName=Root}">
        <Border x:Name="GlowBorder"
                BorderThickness="1"
                CornerRadius="6"
                BorderBrush="{DynamicResource SystemAccentColorPrimaryBrush}"
                Opacity="0"
                IsHitTestVisible="False"
                Margin="-2">
            <Border.Effect>
                <DropShadowEffect Color="{DynamicResource SystemAccentColor}"
                                  BlurRadius="18"
                                  ShadowDepth="0"
                                  Opacity="1"/>
            </Border.Effect>
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsGlowActive, ElementName=Root}"
                                     Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         To="1"
                                                         Duration="0:0:0.05"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         To="0"
                                                         Duration="0:0:0.4"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
        <Button x:Name="SelectorButton"
                Click="SelectorButton_Click"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Padding="12,5,8,5"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                BorderThickness="1"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                HorizontalContentAlignment="Stretch">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="BaseBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"/>
                        <Border x:Name="HoverBorder"
                                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Opacity="0"/>
                        <Border x:Name="PressedBorder"
                                Background="{DynamicResource ControlFillColorTertiaryBrush}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Opacity="0"/>
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1"
                                                         Duration="0:0:0.25">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="HoverBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0"
                                                         Duration="0:0:0.35">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="PressedBorder" Property="Opacity" Value="1"/>
                            <Setter TargetName="HoverBorder" Property="Opacity" Value="0"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="BaseBorder" Property="Opacity" Value="0.5"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorDisabledBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
            <Grid>
                <Grid.Resources>
                    <local:NullToBooleanConverter x:Key="NullToBooleanConverter"/>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ItemsControl Grid.Column="0"
                              ItemsSource="{Binding ItemsSource, ElementName=Root}"
                              Margin="0,0,16,0"
                              Visibility="Hidden"
                              IsHitTestVisible="False"
                              Focusable="False">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Grid/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ContentPresenter Content="{Binding}" VerticalAlignment="Center">
                                <ContentPresenter.Resources>
                                    <local:DisplayMemberConverter x:Key="DisplayMemberConverter"/>
                                    <local:TemplateFallbackConverter x:Key="TemplateFallbackConverter"/>
                                    <DataTemplate x:Key="DefaultTemplate">
                                        <TextBlock VerticalAlignment="Center">
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{StaticResource DisplayMemberConverter}">
                                                    <Binding Path="."/>
                                                    <Binding Path="DisplayMemberPath" RelativeSource="{RelativeSource AncestorType=local:StandardDropdown}"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </DataTemplate>
                                </ContentPresenter.Resources>
                                <ContentPresenter.ContentTemplate>
                                    <MultiBinding Converter="{StaticResource TemplateFallbackConverter}">
                                        <Binding Path="SelectedItemTemplate" RelativeSource="{RelativeSource AncestorType=local:StandardDropdown}"/>
                                        <Binding Path="ItemTemplate" RelativeSource="{RelativeSource AncestorType=local:StandardDropdown}"/>
                                        <Binding Source="{StaticResource DefaultTemplate}"/>
                                    </MultiBinding>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ContentPresenter Grid.Column="0"
                                  Content="{Binding ButtonContent, ElementName=Root}"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Left"
                                  Margin="0,0,16,0">
                    <ContentPresenter.Style>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Visibility"
                                    Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ButtonContent, ElementName=Root, Converter={StaticResource NullToBooleanConverter}}"
                                             Value="True">
                                    <Setter Property="Visibility"
                                            Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentPresenter.Style>
                </ContentPresenter>
                <ContentPresenter Grid.Column="0"
                                  Content="{Binding SelectedItem, ElementName=Root}"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Left"
                                  Margin="0,0,16,0">
                    <ContentPresenter.Style>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Visibility"
                                    Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ButtonContent, ElementName=Root, Converter={StaticResource NullToBooleanConverter}}"
                                             Value="True">
                                    <Setter Property="Visibility"
                                            Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentPresenter.Style>
                    <ContentPresenter.Resources>
                        <local:DisplayMemberConverter x:Key="DisplayMemberConverter"/>
                        <local:TemplateFallbackConverter x:Key="TemplateFallbackConverter"/>
                        <DataTemplate x:Key="DefaultTemplate">
                            <TextBlock VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis">
                                <TextBlock.Text>
                                    <MultiBinding Converter="{StaticResource DisplayMemberConverter}">
                                        <Binding Path="."/>
                                        <Binding Path="DisplayMemberPath"
                                                 RelativeSource="{RelativeSource AncestorType=local:StandardDropdown}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ContentPresenter.Resources>
                    <ContentPresenter.ContentTemplate>
                        <MultiBinding Converter="{StaticResource TemplateFallbackConverter}">
                            <Binding Path="SelectedItemTemplate"
                                     ElementName="Root"/>
                            <Binding Path="ItemTemplate"
                                     ElementName="Root"/>
                            <Binding Source="{StaticResource DefaultTemplate}"/>
                        </MultiBinding>
                    </ContentPresenter.ContentTemplate>
                </ContentPresenter>
                <ui:SymbolIcon Grid.Column="1"
                               Symbol="ChevronDown24"
                               FontSize="12"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               VerticalAlignment="Center"
                               RenderTransformOrigin="0.5,0.5">
                    <ui:SymbolIcon.RenderTransform>
                        <RotateTransform Angle="0"/>
                    </ui:SymbolIcon.RenderTransform>
                    <ui:SymbolIcon.Style>
                        <Style TargetType="{x:Type ui:SymbolIcon}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsOpen, ElementName=MenuPopup}"
                                             Value="True">
                                    <DataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                                 To="180"
                                                                 Duration="0:0:0.18">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.EnterActions>
                                    <DataTrigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                                 To="0"
                                                                 Duration="0:0:0.16">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.ExitActions>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ui:SymbolIcon.Style>
                </ui:SymbolIcon>
            </Grid>
        </Button>

        <menu:SelectorMenuPopup x:Name="MenuPopup"
                                PlacementTarget="{Binding ElementName=SelectorButton}"
                                ItemsSource="{Binding ItemsSource, ElementName=Root}"
                                DisplayMemberPath="{Binding DisplayMemberPath, ElementName=Root}"
                                SelectedItem="{Binding SelectedItem, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                SelectedIndex="{Binding SelectedIndex, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                SelectionChanged="MenuPopup_SelectionChanged"
                                ItemTemplate="{Binding ItemTemplate, ElementName=Root}"
                                MaxPopupWidth="{Binding PopupMaxWidth, ElementName=Root}"
                                MaxPopupHeight="{Binding PopupMaxHeight, ElementName=Root}"/>
    </Grid>
</UserControl>
